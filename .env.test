# Server Configuration
PORT=5001
NODE_ENV=test
BASE_URL=http://localhost:5001

# MongoDB (usando mongodb-memory-server para tests)
MONGO_URL=mongodb://localhost:27017/sushi-test

# Dialogflow (puedes usar mocks para tests)
DIALOGFLOW_PROJECT_ID=test-project-id
GOOGLE_APPLICATION_CREDENTIALS=./credentials/test-credentials.json

# Disable real external services during tests
USE_MOCKS=true